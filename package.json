{
  "name": "jtbit",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "echo 'Build skipped - using direct deployment'",
    "deploy": "wrangler pages deploy src --project-name jtbit"
  },
  "dependencies": {
    "hono": "^4.6.14"
  }
}
